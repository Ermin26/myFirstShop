<% layout('layouts/boilerplate') %>
    <title> Salester-Shop</title>


    
    <div class="row d-flex flex-row flex-wrap justify-content-center" style="margin-bottom: 20vh;">
    <% for(let info of shirts) {%>
            <div class="col-6 col-md-4 col-lg-3 mt-5">
                <div class="card shadow">
                    <div class="card-header text-center">
                        <a href="/product/<%= info.id %> "> <img id="imgSize" class="img-fluid" src="<%= info.p_imgdestination[0] %>"
                                alt="Here goes product img"></a>
                    </div>
                    <div id="body" class="card-body p-3">
                        <h3 class="d-inline">
                            <%= info.p_name %>
                        </h3>
                        <h5 id="pDesc" class="d-inline-flex p-2">
                            - <%= info.p_desc %>
                        </h5>
                    </div>
                    <div class="card-footer p-3">
                        <h2>
                            <%= info.p_price %> €
                        </h2>
                    </div>
                </div>
            </div>
            <% } %>
        </div>